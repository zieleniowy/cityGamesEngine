import React from 'react';
import { useSelector } from 'react-redux';


export default props => {
    // const { i18n, Prizes } = useSelector(state=>({ i18n: state.i18n.lootboxes, Prizes: state.components.  }));
    return (
        <div>rozszerzenie</div>
    )
}